- deploy script should simply call autodeploy.sh script
- run sound from a container

- log last 10 played songs

- get the title of radio paradise song
- buttons should definitely not refresh our site
- css for buttons
- automount for ANY USB

options to find devices:

dh -aTh
sudo fdisk -l
lsblk -f
ls -la /dev/disk/by-uuid

should edit /etc/fstab

sudo mount -a

mplayer vs slave mode?
mplayer -slave -input file="./mplayer_commands" -idle -shuffle -playlist playlist &

